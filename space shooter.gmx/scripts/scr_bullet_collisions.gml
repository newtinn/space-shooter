/*if (distance_to_object(obj_boss1) <= 1) {
    hit = instance_create(x,y,obj_bullet_hit);
    hit.image_angle = image_angle;
    with(self) instance_destroy();
    obj_boss1.hit = true;
    if (global.boss1health > 0) global.boss1health -= 1;
}*/
